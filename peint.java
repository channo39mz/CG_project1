import java.awt.*;
import java.awt.image.BufferedImage;

public class peint {
    static void colorpent(BufferedImage buffer){
        Color c = new Color(217,234,196);
        DrawALGO.floodFill(buffer, 186, 216, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 140, 484, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 131, 173, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 96, 280, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 116, 375, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 295, 406, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 286, 453, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 372, 472, Color.WHITE, c);

        c = new Color(254,247,227);
        DrawALGO.floodFill(buffer, 270, 144, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 140, 134, Color.WHITE, c);
        c = new Color(254,247,233);
        DrawALGO.floodFill(buffer, 190, 428, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 370, 500, Color.WHITE, c);
        c = new Color(181,196,163);
        DrawALGO.floodFill(buffer, 170, 205, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 164, 195, Color.WHITE, c);
        c = new Color(140,155,141);
        DrawALGO.floodFill(buffer, 215, 264, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 136, 270, Color.WHITE, c);
        c = new Color(177,211,157);
        DrawALGO.floodFill(buffer, 140, 277, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 222, 268, Color.WHITE, c);
        c = new Color(98,72,95);
        DrawALGO.floodFill(buffer, 215, 240, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 133, 248, Color.WHITE, c);
        c = new Color(0,0,0);
        DrawALGO.floodFill(buffer, 157, 356, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 205, 342, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 182, 367, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 224, 356, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 254, 343, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 243, 340, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 247, 349, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 248, 351, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 276, 338, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 299, 357, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 322, 357, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 333, 355, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 352, 330, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 373, 349, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 402, 346, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 410, 330, Color.WHITE, c);
        c = new Color(255,193,199);
        DrawALGO.floodFill(buffer, 164, 390, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 188, 360, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 210, 350, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 230, 347, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 304, 343, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 378, 336, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 394, 342, Color.WHITE, c);
        c = new Color(253,208,185);
        DrawALGO.floodFill(buffer, 255, 279, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 119, 289, Color.WHITE, c);
        c = new Color(242,159,4);
        DrawALGO.floodFill(buffer, 377, 200, Color.WHITE, c);
        c = new Color(242,48,4);
        DrawALGO.floodFill(buffer, 40, 42, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 551, 255, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 147, 577, Color.WHITE, c);
        c = new Color(242,116,4);
        DrawALGO.floodFill(buffer, 26, 128, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 442, 52, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 592, 591, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 8, 457, Color.WHITE, c);
        c = new Color(128,44,17);
        DrawALGO.floodFill(buffer, 10, 331, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 437, 590, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 564, 116, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 135, 14, Color.WHITE, c);
        c = new Color(179,61,24);
        DrawALGO.floodFill(buffer, 10, 594, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 593, 419, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 250, 20, Color.WHITE, c);
        c = new Color(254,210,195);
        DrawALGO.floodFill(buffer, 53, 189, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 354, 200, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 322, 205, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 31, 253, Color.WHITE, c);
        DrawALGO.floodFill(buffer, 54, 280, Color.WHITE, c);
    }
}
